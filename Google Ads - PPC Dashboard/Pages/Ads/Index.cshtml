@page
@model Google_Ads___PPC_Dashboard.Pages.Ads.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].AdGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].Headline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].AdType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].URL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ad[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Ad) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdGroup.AdGroupId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Headline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.URL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AdId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AdId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AdId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
